._*
.DS_Store
node_modules
dist
build
.env
.vercel
.env*.local

# Local-only artifacts
.playwright-mcp/
*.bak
# Loose screenshots at the repo root only (assets under client/ stay tracked).
/*.png
# Redesign reference captures (Contra/Upwork screenshots).
tmp/
